name: larmor
channels:
  - conda-forge
dependencies:
  - python=3.11
  - jupyterlab
  - matplotlib
  - numpy
  - scipy
  - pip
  - pytest
  - pip:
      - mrsimulator
      - lmfit
      - csdmpy
      - nmrglue
      - fastapi
      - uvicorn[standard]
      - PySide6
      - pyqtgraph
      - requests            # the ? ▸ More… easter egg (degrades gracefully if absent)
      # install LARMOR itself (editable, from this repo)
      - -e .
